import { View } from "react-native";

import Colors from "@/constants/Colors";

const Separator = () => {
  return (
    <View style={{ borderBottomWidth: 1, borderBottomColor: Colors.c050 }} />
  );
};

export default Separator;
